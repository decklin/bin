#!/bin/sh

test -t 0 && frame=-nw || frame=-c
test -n "$*" || set -- -e nil

# in case we haven't fixed sudo to preserve this env var, need
# to be able to find our socket
test -z "$TMPDIR" -a -e "$HOME/tmp" && export TMPDIR="$HOME/tmp"

exec emacsclient "$frame" -a emacsnw "$@"
