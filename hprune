#!/usr/bin/python

import sys
import Levenshtein as lev

SEARCH = 10
CUTOFF = 5

if __name__ == '__main__':
    lines = sys.stdin.readlines()

    for i in range(0, len(lines)):
        for j in range(i + 1, min(i + SEARCH, len(lines))):
            if lev.distance(lines[i], lines[j]) < CUTOFF:
                break
        else:
            print lines[i],
