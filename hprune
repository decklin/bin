#!/usr/bin/python

import sys
import Levenshtein as lev

SEARCH = 10
CUTOFF = 5

if __name__ == '__main__':
    lines = sys.stdin.readlines()
    print lines[0],

    for i in range(1, len(lines)):
        for j in range(max(1, i - SEARCH), i):
            if lev.distance(lines[i], lines[j]) < CUTOFF:
                break
        else:
            print lines[i],
